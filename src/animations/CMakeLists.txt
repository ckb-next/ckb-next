if (WITH_GRADIENT)
    add_subdirectory(gradient)
endif ()
if (WITH_HEAT)
    add_subdirectory(heat)
endif ()
if (WITH_PINWHEEL)
    add_subdirectory(pinwheel)
endif ()
if (WITH_RAIN)
    add_subdirectory(rain)
endif ()
if (WITH_RANDOM)
    add_subdirectory(random)
endif ()
if (WITH_RIPPLE)
    add_subdirectory(ripple)
endif ()
if (WITH_WAVE)
    add_subdirectory(wave)
endif ()
if (WITH_MVIZ)
    add_subdirectory(mviz)
endif ()
